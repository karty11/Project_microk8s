apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-observability
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  demo-observability-dashboard.json: |
    {{ .Files.Get "k8s/grafana/dashboards/demo-observability-dashboard.json" | nindent 4 }}
